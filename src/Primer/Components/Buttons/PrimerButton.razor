@namespace Primer.Components
@inherits PrimerComponentBase

@if (Visible)
{
    <PrimerDynamicElement Tag="@As" type="button" class="@ClassGenerator.Class" @onclick="OnClick" aria-selected="@Selected.ToString().ToLower()" aria-disabled="@Disabled.ToString().ToLower()">
        @ChildContent
    </PrimerDynamicElement>
}
